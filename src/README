groovy/
   Empty.

java/
   Contains a file downloaded from somewhere that fixes a glitch in Groovy's
   MySQL driver related to Unicode.  When Groovy creates tables, it normally
   creates them in Latin-1; this fixes them to use UTF-8.

sass/
   Generated by Grails, part of look-and-feel.

templates/
   Scaffolding code.

   artifacts/
      Used only to generate new code.

   scaffolding/
      Code added at runtime, generated by stuff in `artifacts'.

   war/
      Only `web.xml', used to generate the .war file.
