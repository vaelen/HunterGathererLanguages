@artifact.package@class @artifact.name@ {

    // Following two clauses added by Andrew Young.

    // Used by "export to PDF/Excel/..." (plugins added in config file)
    def exportService

    def scaffold = true

}
